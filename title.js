document.addEventListener('DOMContentLoaded', function() {
    var options = {
        strings: ['Here:)'],
        typeSpeed: 120, //especificação da valecidade de digitação
        loop: false  // definição se o efeito se repete ou não
    };

    var typed = new Typed('#here', options);
});
